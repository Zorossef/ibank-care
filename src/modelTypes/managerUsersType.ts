import { FC } from "react";

export type UserItemType = {
  name: string;
  userName: string;
  AccountNumber: string;
  balance: string;
  branch: string;
  swiftCode: string;
  hasAction?: boolean;
};
